import React from 'react';
// Correct the import path to point to the components directory
import Layout from "../components/Layout"; // Assuming LeaderboardPage is in components

const Index = () => {
  return <Layout />; // Render the LeaderboardPage component
};

export default Index;
